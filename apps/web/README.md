# Web App

Vite + React client for the Clash visual editor.

## Responsibilities

- render the graph editor
- manage workspace UX
- preview and publish derived YAML
- call same-origin serverless APIs for workspace and publish storage

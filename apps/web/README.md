# Web App

Vite + React client for the Clash visual editor.

## Responsibilities

- render the graph editor
- manage workspace UX
- export and import project data
- preview and publish derived YAML
- call same-origin serverless APIs for workspace storage, publish, formatter, and source inspection

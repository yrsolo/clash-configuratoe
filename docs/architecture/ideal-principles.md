# Ideal Principles

The repository follows a static-first, incremental architecture.

## Principles

- JSON project data is the canonical source of truth.
- YAML is always a derived artifact.
- Secret-bearing operations stay behind a serverless boundary.
- The web app stays focused on editing, preview, and workspace UX.
- Operational context belongs in `work/`, not in permanent docs.
